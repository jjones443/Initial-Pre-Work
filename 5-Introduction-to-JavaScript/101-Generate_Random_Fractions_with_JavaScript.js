// In this lesson I learned to generate a random fraction using the Math.random() function

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}