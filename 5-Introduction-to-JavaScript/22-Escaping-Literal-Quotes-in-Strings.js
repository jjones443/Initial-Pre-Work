// In this lesson I learned that you can use the backslash symbol \ to prevent JavaScript from thinking a quote in text is the end of a string. This is done by putting \" \' when using quotes in text.

var myStr = "I am a \"double quoted\" string inside \"double quotes\"."; // Change this line