// In this lesson I learned to discover boolean values in an easier way than if/else statements

function isLess(a, b) {
  // Fix this code
 return a < b;
}

// Change these values to test
isLess(10, 15);