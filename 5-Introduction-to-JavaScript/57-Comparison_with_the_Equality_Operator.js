// In this lesson I learned to use the equality operator == to compare values of items using the boolean if condition

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);