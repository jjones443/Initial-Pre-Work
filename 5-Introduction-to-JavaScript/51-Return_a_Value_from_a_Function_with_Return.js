/* In this lesson I learned that a function can be used to return a value by assigning an argument to the funtion as
well as a set value within the function */

// Example
function minusSeven(num) {
  return num - 7;
}

// Only change code below this line

function timesFive(num) {
  return num * 5;
}

console.log(timesFive(10));